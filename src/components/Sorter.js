//This file is meant to hold the sorter component, aswell as the list of algorithms

//the sorter function should instantly run, and then provide a "playback" object that allows the user to stop start...
//and go backwards through the function

import React from 'react'

const Sorters(){
    
}

export default Sorter